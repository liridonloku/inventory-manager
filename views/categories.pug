extends layout 

block content 
  h1=title 
  ul 
    each category in categories 
      li 
        p 
          a(href=category.url) #[strong #{category.name}]
    else 
      p No categories.

  a(class='btn btn-primary' href='/inventory/categories/create') New
